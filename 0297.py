sets = {"Hello", 500, True, "Bye", 33.5}

print(sets)

# Класс set (множество) — это одна из ключевых структур данных в Python. Она представляет собой неупорядоченную коллекцию уникальных элементов.

# Каждый элемент представлен ровно один раз, дубликаты исключены.